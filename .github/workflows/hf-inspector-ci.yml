name: CI

on:
  push:
    paths:
      - hf-inspector/**
      - .github/workflows/hf-inspector-ci.yml
    branches:
      - main

  pull_request:
    paths:
      - hf-inspector/**
      - .github/workflows/hf-inspector-ci.yml
    branches:
      - main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

          cache-dependency-path: hf-inspector/pnpm-lock.yaml
      - name: Install
        run: pnpm install
        working-directory: hf-inspector

      - name: Lint
        run: pnpm run lint
        working-directory: hf-inspector

  build-test:
    name: Build Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

          cache-dependency-path: hf-inspector/pnpm-lock.yaml
      - name: Install
        run: pnpm install
        working-directory: hf-inspector

      - name: Build
        run: |
        working-directory: hf-inspector
          pnpm run build

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

          cache-dependency-path: hf-inspector/pnpm-lock.yaml
      - name: Install
        run: pnpm install
        working-directory: hf-inspector

      - name: Build
        run: pnpm run build
        working-directory: hf-inspector

      - name: Typecheck
        run: pnpm run typecheck
        working-directory: hf-inspector
