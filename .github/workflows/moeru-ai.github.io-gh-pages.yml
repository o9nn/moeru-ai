name: gh-pages

on:
  push:
    paths:
      - moeru-ai.github.io/**
      - .github/workflows/moeru-ai.github.io-gh-pages.yml
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: importantimport/.github/.github/workflows/pnpm-gh-pages-artifact.yml@main
