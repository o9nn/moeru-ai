name: CI

on:
  push:
    paths:
      - gpuu/**
      - .github/workflows/gpuu-ci.yml
    branches:
      - main

  pull_request:
    paths:
      - gpuu/**
      - .github/workflows/gpuu-ci.yml
    branches:
      - main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

          cache-dependency-path: gpuu/pnpm-lock.yaml
      - name: Install
        run: pnpm install
        working-directory: gpuu

      - name: Lint
        run: pnpm run lint
        working-directory: gpuu

  build-test:
    name: Build Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

          cache-dependency-path: gpuu/pnpm-lock.yaml
      - name: Install
        run: pnpm install
        working-directory: gpuu

      - name: Build
        run: |
        working-directory: gpuu
          pnpm run build

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: pnpm

          cache-dependency-path: gpuu/pnpm-lock.yaml
      - name: Install
        run: pnpm install
        working-directory: gpuu

      - name: Typecheck
        run: pnpm run typecheck
        working-directory: gpuu
