name: gh-pages

on:
  push:
    paths:
      - talk/**
      - .github/workflows/talk-gh-pages.yml
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: importantimport/.github/.github/workflows/pnpm-gh-pages-artifact.yml@main
